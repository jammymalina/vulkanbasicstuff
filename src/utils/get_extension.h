#ifndef GET_EXTENSION_H
#define GET_EXTENSION_H

#include <string.h>

const char *get_filename_ext(const char *filename);
const char *get_filename_extn(const char *filename, int index);

#endif // GET_EXTENSION_H
